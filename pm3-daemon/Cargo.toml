[package]
name = "pm3-daemon"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.49.0", features = ["rt-multi-thread", "macros", "process", "fs", "time", "sync", "net", "io-util", "signal"] }
sysinfo = "0.30"
anyhow = "1.0"
rand = "0.8"
os_pipe = "1.0"
